# generated: 2026-04-09
# last_updated: 2026-04-11 # story 3-4 in-progress → review after dev kick-back fix pass v1.2.0. BLOCKER #1 (pause-loop reconnect generated duplicate questions) fixed in sse.py:stream_session via existing-question replay branch — when count_answers==0 and Q1 already in DB, the loop now REPLAYS from the in-memory text registry instead of calling append_question (which used max(number)+1). BLOCKER #2 (frontend collapsed answer_out_of_order 409 into "already answered" string) fixed in ChatPanel.tsx:submitAnswer by parsing response.clone().json() detail.error and branching on the structured error code. Regression tests added per kick-back instructions #4+#5: TestReconnectReplay (2 tests) + test_post_q1_succeeds_when_db_has_unanswered_q1_q2_q3 + test_post_q3_when_no_answers_returns_out_of_order_not_already_submitted + 3 new ChatPanel.answerFlow tests. 296 API + 103 web tests passing; lint + build clean. NEXT: re-run bmad-code-review on 1.2.0 changes before re-handing to Project Lead for manual QA. Previous line preserved for audit: 3-4 KICKED BACK awaiting-manual-qa → in-progress on 2026-04-11 with 2 blockers (#1 pause-loop, #2 false-positive 409); items 13/14 pre-validated via schema inspection. Full HALT report in story file.
# project: helprs
# project_key: NOKEY
# tracking_system: file-system
# story_location: {project-root}/_bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - awaiting-manual-qa: Code review automated done + patches applied.
#     Awaiting Project Lead manual smoke test sign-off.
#     CANNOT transition to `done` until the story's Manual QA Checklist
#     is signed by the Project Lead. Blocks creation of next story.
#   - done: Story completed (automated tests + manual QA both signed off)
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - SM typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Stories transitioning from 'review' to 'done' MUST pass through 'awaiting-manual-qa'.
#   The code-review workflow HALTs at this state and hands off to the Project Lead
#   explicitly. Only the Project Lead can set a story to 'done' (see epic-2 retro
#   action items #1-3, 2026-04-10).
# - bmad-create-story refuses to create a new story if any story is currently in
#   'awaiting-manual-qa', unless invoked with `--skip-manual-qa-gate <reason>`.

generated: 2026-04-09
last_updated: 2026-04-11 # 3-4 KICKED BACK v2: awaiting-manual-qa → in-progress (second kick-back of the same story). Project Lead second manual QA pass found 2 new BLOCKERs after v1.2.0 fix pass + v2 code review correctly closed original BLOCKERs #1 and #2. **Happy path (items 1-8) + DB invariants (13-15) + auto-advance (20-21) + keyboard (26-28) + error paths (29-31) + responsive (35-36) all PASS**. v1.2.0 fixes hold: fresh session shows only one question streaming at a time, POST /answers for correct question returns 200 + streams feedback, no duplicate questions on reconnect. NEW BLOCKERs: (#3) resume race in ChatPanel.tsx:137-151 seeding useEffect — when SSE first-token lands before seeding effect runs (reliable 300-500ms race), messages.length > 0 and the effect bails via the "already seeded" guard without seeding placeholders for answered progress entries, so user loses visual context of completed Q1/A1/F1 on reload; (#4) message ordering Q_next before F_current — backend race between the POST /answers feedback stream and the GET /stream pause-loop, because v1.1.0 moved db.commit() BEFORE the feedback stream to make count_answers visible, the pause-loop advances immediately after persist instead of after feedback commit, so Q3 starts streaming ~5s before F2 finishes, rendering order in the chat becomes Q/A/Q_next/F instead of Q/A/F/Q_next. Non-blocking findings: NFR4 latency violated (6.25s real Claude vs 5s target), LLM prompt doesn't emit `path:line` backticks so code-link rendering is unreachable in practice, white-void CSS in tabbed layout, "Generating feedback..." stuck on question-stream error, dirty session 2606dbba grew 3→14 questions during dev reload tests (v1.2.0 replay doesn't cover pre-API-restart sessions). Polish items DEFERRED to epic-3 polish pass per Project Lead decision: 9-12 (code links), 22-25 (reduced motion), 32-33 (LLM error paths complex), 37-38 (deferred 3-3 bugs carry-forward). Also checklist item 30 wording stale: v1.1.0 introduced answer_out_of_order which fires FIRST, making answer_already_submitted unreachable via sequential POST — dev should update wording. Full report + fix directions (Options A/B/C for each blocker) + required regression tests in story file "Manual QA Results — 2026-04-11 (second pass, kicked back to in-progress)" section. Previous line preserved for audit: 3-4 v1.2.0 re-review complete — bmad-code-review second pass applied 6 patches, 292→124 API + 103 web tests passing, status awaiting-manual-qa.
project: helprs
project_key: NOKEY
tracking_system: file-system
story_location: "{project-root}/_bmad-output/implementation-artifacts"

development_status:
  # Epic 1: GitHub App Installation & Identity
  epic-1: done
  1-1-project-scaffolding-and-deployment-infrastructure: done
  1-2-core-backend-infrastructure: done
  1-3-github-oauth-and-user-identity: done
  1-4-github-app-installation-and-webhook-registration: done
  1-5-byok-configuration-and-admin-settings-ui: done
  epic-1-retrospective: done

  # Epic 2: Webhook Processing & Session Lifecycle
  epic-2: done
  2-1-webhook-reception-and-event-dispatch: done
  2-2-session-creation-and-pr-comment-posting: done
  epic-2-retrospective: done

  # Epic 3: Socratic Comprehension Experience
  epic-3: in-progress
  # 3-1 was bundled with story 3-2's PR (2026-04-10) — code review found
  # that 3-1's backend had silently been marked `done` without passing the
  # `awaiting-manual-qa` gate (epic-2 retro action items #1-3). Reverted
  # to `awaiting-manual-qa`; the Project Lead's Manual QA for story 3-2
  # covers story 3-1's ACs as well (see 3-2 Manual QA Checklist section
  # "Coverage for story 3-1 ACs (bundled)").
  #
  # MANUAL QA BYPASS — 2026-04-10
  # ==============================
  # Project Lead (Marius) explicitly accepted bypassing the manual QA gate
  # for BOTH stories 3-1 and 3-2. The 27 checklist items in
  # `3-2-split-view-session-ui-and-diff-viewer.md` (lines 611–670) are
  # INTENTIONALLY LEFT UNCHECKED as proof that the checklist was not
  # executed. This is a knowing, traceable skip — NOT a silent chain-through
  # like the Epic 1/2 failure mode that the `awaiting-manual-qa` gate was
  # created to prevent.
  #
  # Rationale given: front-end split-view + backend read-only session API
  # will be exercised end-to-end during Story 3-3 (question generation +
  # SSE streaming) manual QA, which depends on both 3-1 and 3-2 working.
  # If 3-3's Manual QA surfaces a regression that would have been caught
  # here, that regression becomes a new P0 bug story blocking 3-3.
  #
  # This bypass decision is restricted to stories 3-1 and 3-2. It does
  # NOT set a precedent. The default behaviour for future stories remains:
  # HALT at `awaiting-manual-qa`, hand off explicitly, wait for sign-off.
  3-1-comprehension-domain-model-and-session-api: done
  # bmad-code-review ran 2026-04-10: 10 patches applied automatically, 4
  # remained as action items. Second dev pass 2026-04-10 resolved all 4.
  # Second-pass bmad-code-review ran 2026-04-10: 2 decision-needed + 5
  # patches + 6 deferred + 7 dismissed. Decisions resolved: (D1/Critical)
  # REMOVED the custom onSeparatorKeyDown because react-resizable-panels v4
  # already does a 5% step via a native keydown listener — the custom
  # handler was a duplicate that caused store/DOM drift, hidden by a test
  # mock that replaced the real Separator; (D2/Minor) transient 429 during
  # background refetch wipes UI state → deferred, revisit with Story 3.3
  # SSE lifecycle. Patches applied: (P1) removed custom keyboard handler +
  # 3 vacuous tests + mock onKeyDown forwarding, (P2) refined
  # DiffViewer.isBinaryFile heuristic — excludes renames/copies, documented
  # mode-only false positive as deferred (gitdiff-parser's isBinary flag
  # is empirically never set), (P3) added 422/429 regression tests,
  # (P4) added role=region + aria-label to binary placeholder, (P5)
  # replaced "Story 3.3 backlog" prose with grep-able TODO(story-3.3).
  # make lint && vitest = clean + 24 web tests green (24 = 25 −3 +2).
  3-2-split-view-session-ui-and-diff-viewer: done
  # Manual QA completed 2026-04-11 by Project Lead. 20/26 items checked,
  # 6 explicitly skipped per Option 3 scoping (rationale documented in
  # the story file's "Manual QA Results" section). QA session surfaced
  # AND fixed 3 bugs: (1) Epic 1 regression — get_installations_for_user
  # called /user/installations which 403s for OAuth-App tokens; refactored
  # to use local DB + /user/orgs and added read:org scope; (2) Story 3-3
  # SSE auth wiring — useSSE doc claimed cookie auth but JWT was in
  # memory; ChatPanel built relative URL; get_current_user only read
  # header; fixed via query-param fallback + absolute URL. (3) Story 3-2
  # dark-mode CSS — react-diff-view default theme unreadable on dark
  # surface; overrode --diff-* CSS vars + reordered import cascade.
  # 4 deferred follow-ups filed (no SSE replay on resume, StrictMode
  # double-mount rollback, misleading "Connection lost" for 4xx,
  # markdown tokenize warning). NONE of these block 3-3 done.
  3-3-question-generation-and-sse-streaming: done
  # 2026-04-11: SECOND kick-back awaiting-manual-qa → in-progress by Project
  # Lead. v1 HALT + v1.2.0 fix pass + v2 code review + v2 manual QA =
  # original BLOCKERs #1/#2 closed, but TWO NEW BLOCKERs surfaced during
  # full checklist walkthrough. See story file "Manual QA Results —
  # 2026-04-11 (second pass, kicked back to in-progress)" section for
  # full report (happy path + pass/fail table + blockers + fix directions
  # + required regression tests + evidence archive).
  #
  # WHAT PASSED:
  #   - Happy path items 1-8 (submit → feedback → auto-advance to Q2)
  #   - DB invariants items 13-15 (schema, hash format, no text leak in logs)
  #   - Auto-advance items 20-21 (Q_next auto-streams, no Next button)
  #   - Keyboard items 26-28 (shift+enter, enter empty, enter disabled)
  #   - Error paths items 29-31 (404/409/422 with correct error codes)
  #   - Responsive items 35-36 (tabbed layout at ~900px, mobile nudge at ~600px)
  #   - v1.2.0 fixes for original BLOCKERs #1/#2 hold on fresh sessions
  #
  # NEW BLOCKER #3: resume race in ChatPanel.tsx:137-151 seeding useEffect.
  #   On reload with 1+ answered questions, the SSE first-token frame lands
  #   300-500ms BEFORE the seeding effect runs (the effect gates on session
  #   being loaded via async query). messages.length > 0 at effect runtime
  #   → bail branch fires → seededForSessionRef set → placeholders never
  #   seeded. User sees only the currently-streaming Q_next with no history
  #   of completed Q/A/F cycles, and header shows "Question 0 of N" instead
  #   of "N completed". Backend is correct: GET /sessions/{id}.progress
  #   returns {status: "answered"} for Q1. Fix = tighten the bail condition
  #   from "messages.length > 0" to "messages.some(m => m.id.startsWith('resume-q-'))".
  #
  # NEW BLOCKER #4: message ordering Q_next before F_current. Backend race
  #   between POST /answers feedback stream and GET /stream pause-loop.
  #   v1.1.0 moved db.commit() BEFORE feedback stream (to make count_answers
  #   visible to the polling pause-loop). Pause-loop unblocks on persist,
  #   not on feedback commit. Timing: Q2 answered at 16:10:33.001, Q3 committed
  #   at 16:10:38.281 — 5.28s race window while F2 is still streaming. Q3
  #   frames land in the store with earlier indices than F2's commit event.
  #   Rendering order becomes Q/A/Q_next/F instead of Q/A/F/Q_next.
  #   Fix = introduce feedback_committed_for_question_N flag in answer_pubsub
  #   registry and make pause-loop poll that instead of count_answers directly.
  #
  # REQUIRED REGRESSION TESTS (called out in kick-back instructions):
  #   (a) ChatPanel.resume.test.tsx integration test: render with mocked
  #       session.progress = [{number:1, status:'answered'}] + mock SSE
  #       emitting Q2 tokens immediately, assert both resume-q-1 placeholders
  #       AND streaming Q2 coexist in messages[].
  #   (b) test_answer_submission.py backend test: POST answer to Q1, start
  #       a second GET /stream IMMEDIATELY (before feedback consumed), assert
  #       second stream does NOT advance to Q2 until feedback commit is done.
  #
  # NON-BLOCKING FINDINGS (dev to log in deferred-work.md):
  #   - NFR4 latency violated: real Claude API for ~300-token feedback takes
  #     6.25s (TTFB 506ms is fine for NFR3). Options: retune prompt, relax
  #     NFR4 to split target, or use Haiku for feedback.
  #   - LLM feedback prompt doesn't emit `path:line` in backticks → code-link
  #     UX (AC#9-11) is unreachable in practice with real Claude. Frontend
  #     rendering code is correct (fixture tests pass) but no end-to-end
  #     integration. Fix = prompt engineering + few-shot examples.
  #   - Checklist item 30 wording stale: v1.1.0 introduced answer_out_of_order
  #     which fires FIRST, making answer_already_submitted unreachable via
  #     sequential POST. Dev should update the checklist wording.
  #   - White-void CSS finding in tabbed layout at ~700-1100px widths.
  #   - "Generating feedback..." input stuck after question-stream error
  #     (observed on dirty session, not reproducible on fresh session but
  #     symptomatic of missing error → re-enable wiring).
  #   - Dirty session 2606dbba grew 3→14 questions during dev reload tests
  #     after v1.2.0 applied. Suggests v1.2.0 replay branch doesn't cover
  #     sessions whose questions were created BEFORE the API container was
  #     restarted (cold registry fallback). Not a 3-4 blocker because prod
  #     sessions will all be post-deploy, but worth an Epic 3 retro discussion.
  #
  # DEFERRED to epic-3 polish pass (Project Lead decision 2026-04-11):
  #   - Items 9-12 (code link click + hover + non-diff file)
  #   - Items 22-25 (reduced motion UX-DR3)
  #   - Items 32-33 (LLM timeout + empty feedback — complex test setup)
  #   - Items 37-38 (deferred 3-3 bugs: two-tab + fake BYOK stream error)
  # These are batched to avoid per-story polish testing overhead. Dev should
  # NOT try to address them in v1.3.0.
  #
  # Fresh PR for re-re-QA (dev can reuse or close+reopen):
  #   https://github.com/mariuspruvot/helprs-qa-test-3-3/pull/3
  3-4-answer-submission-and-feedback-with-code-links: in-progress
  3-5-role-adaptation-beyond-diff-and-large-pr-handling: backlog
  epic-3-retrospective: optional

  # Epic 4: Scoring, Quality Signals & Session Completion
  epic-4: backlog
  4-1-comprehension-scoring-and-score-card-ui: backlog
  4-2-github-status-check-question-reporting-and-post-session-feedback: backlog
  epic-4-retrospective: optional

  # Epic 5: Demo Experience & Landing Page
  epic-5: backlog
  5-1-demo-session-and-pre-loaded-experience: backlog
  5-2-landing-page-and-install-conversion: backlog
  epic-5-retrospective: optional

  # Epic 6: Billing & Subscriptions
  epic-6: backlog
  6-1-billing-model-and-lemon-squeezy-integration: backlog
  epic-6-retrospective: optional
